# 모바일 청첩장 구현 명세서

## 1. 프로젝트 목표

Next.js와 TypeScript를 사용하여 모바일 환경에 최적화된 웹 청첩장을 구현한다.

전체적인 디자인은 **깔끔하고 미니멀한 분위기**로 구성한다. 과도한 장식이나 화려한 애니메이션은 피하고, 사진과 타이포그래피가 자연스럽게 돋보이도록 만든다.

단순한 시안이나 정적 화면만 만들지 말고, 실제로 배포하여 사용할 수 있는 완성도 높은 모바일 청첩장을 구현한다.

---

## 2. 개발 원칙

* 기존 저장소의 구조와 설정을 우선 확인한 뒤 구현한다.
* 기존 프로젝트가 있다면 불필요하게 초기화하거나 구조를 전면 변경하지 않는다.
* Next.js와 TypeScript를 사용한다.
* 현재 저장소가 App Router 기반이면 App Router 구조를 유지한다.
* 모든 주요 컴포넌트와 데이터 타입을 명확하게 정의한다.
* 가능한 한 `any` 타입을 사용하지 않는다.
* 반복되는 UI와 로직은 재사용 가능한 컴포넌트로 분리한다.
* 모바일 우선 방식으로 구현하되 태블릿과 데스크톱에서도 자연스럽게 보여야 한다.
* 실제 사용할 수 없는 버튼이나 동작하지 않는 가짜 기능을 만들지 않는다.
* 외부 서비스 키가 필요한 기능은 환경변수로 관리하고 `.env.example`을 제공한다.
* 구현 완료 후 lint, typecheck, build를 실행하고 오류를 수정한다.

---

## 3. 디자인 방향

### 전체 분위기

* 깔끔하고 차분한 미니멀 디자인
* 넓은 여백과 정돈된 레이아웃
* 부드럽고 따뜻한 인상
* 모바일에서 읽기 편한 글자 크기와 줄 간격
* 장식적인 요소는 최소화
* 한 화면에 너무 많은 정보를 노출하지 않기
* 섹션 사이의 흐름이 자연스럽게 이어지도록 구성

### 색상

다음과 같은 저채도 색상을 기본으로 사용한다.

* 배경: 아이보리 또는 따뜻한 화이트
* 본문: 짙은 회색
* 보조 텍스트: 중간 회색
* 강조색: 베이지, 세이지 그린 또는 연한 브라운 계열
* 경계선: 매우 연한 회색 또는 베이지

색상 값은 CSS 변수 또는 테마 설정으로 중앙 관리한다.

### 타이포그래피

* 한글 가독성이 좋은 웹폰트를 사용한다.
* 본문용 폰트와 포인트용 폰트는 최대 2종까지만 사용한다.
* 지나치게 얇거나 장식적인 글꼴은 피한다.
* 폰트 로딩으로 인한 레이아웃 이동을 최소화한다.
* Next.js의 폰트 최적화 기능을 우선 사용한다.

### 애니메이션

* 스크롤 진입 시 가벼운 페이드 또는 슬라이드 애니메이션을 적용할 수 있다.
* 애니메이션은 짧고 자연스럽게 처리한다.
* 과도한 패럴랙스, 자동 확대, 튀는 효과는 사용하지 않는다.
* 사용자가 `prefers-reduced-motion`을 설정한 경우 애니메이션을 줄이거나 제거한다.

---

## 4. 기본 청첩장 정보 관리

신랑·신부 정보, 예식 정보, 연락처, 계좌번호, 이미지 경로, 안내 문구 등은 페이지 컴포넌트 곳곳에 직접 작성하지 않는다.

다음과 같이 하나의 설정 파일 또는 타입이 정의된 데이터 파일에서 관리한다.

예시:

```ts
export interface WeddingConfig {
  couple: {
    groom: Person;
    bride: Person;
  };
  wedding: {
    date: string;
    time: string;
    venueName: string;
    hallName?: string;
    address: string;
  };
  invitation: {
    title: string;
    message: string[];
  };
  gallery: {
    src: string;
    alt: string;
  }[];
}

export interface Person {
  name: string;
  phone?: string;
  father?: FamilyMember;
  mother?: FamilyMember;
}
```

최종 구조는 프로젝트에 맞게 개선해도 되지만, 콘텐츠를 한곳에서 쉽게 수정할 수 있어야 한다.

초기 데이터에는 실제 개인정보 대신 아래와 같은 명확한 예시 값을 넣는다.

* 신랑: 김신랑
* 신부: 이신부
* 예식일: 2027년 5월 15일 토요일
* 예식 시간: 오후 1시
* 예식장: 예시 웨딩홀
* 주소: 서울특별시 예시구 예시로 123

개인정보가 필요한 부분에는 사용자가 쉽게 찾을 수 있도록 주석이나 문서로 수정 위치를 안내한다.

---

## 5. 필수 페이지 구성

청첩장은 하나의 긴 스크롤 페이지를 기본으로 구현한다.

### 5.1 메인 커버

첫 화면에는 다음 정보를 표시한다.

* 대표 사진
* 신랑·신부 이름
* 결혼식 날짜
* 결혼식 시간
* 예식장 이름
* 짧은 영문 또는 한글 문구

예시 문구:

```text
우리, 결혼합니다
```

또는:

```text
THE WEDDING DAY
```

대표 사진 위에 텍스트를 표시하는 경우 가독성을 위해 적절한 오버레이를 적용한다.

첫 화면은 모바일 브라우저의 실제 뷰포트 높이를 고려하여 구성하고, 주소창 변화 때문에 화면이 어색하게 잘리지 않도록 한다.

---

### 5.2 초대 문구

신랑·신부가 하객에게 전하는 초대 문구를 표시한다.

예시:

```text
서로 다른 길을 걸어온 두 사람이
이제 같은 곳을 바라보며 함께 걸어가려 합니다.

귀한 걸음으로 축복해 주시면
더없는 기쁨으로 간직하겠습니다.
```

문구는 설정 파일에서 줄 단위 또는 문단 단위로 수정할 수 있게 한다.

---

### 5.3 신랑·신부 및 혼주 정보

다음 정보를 정돈된 형태로 표시한다.

* 신랑 이름
* 신부 이름
* 신랑 측 부모님 성함
* 신부 측 부모님 성함
* 고인 표기 지원
* 필요하면 연락 버튼 표시

가족관계 표기는 자연스러운 한국식 청첩장 형식을 따른다.

예시:

```text
김아버지 · 박어머니의 아들 김신랑
이아버지 · 최어머니의 딸 이신부
```

부모님 정보가 일부 없는 경우에도 레이아웃이 깨지지 않도록 선택 항목으로 처리한다.

---

### 5.4 예식 일시

다음 정보를 표시한다.

* 연도
* 월
* 일
* 요일
* 오전 또는 오후
* 예식 시간
* 예식일까지 남은 날짜

오늘을 기준으로 남은 날짜를 계산하여 다음과 같이 표시한다.

```text
결혼식까지 100일 남았습니다.
```

예식 당일에는 다음과 같이 표시한다.

```text
오늘, 저희 결혼합니다.
```

예식일이 지난 경우에는 부자연스러운 음수 값 대신 별도 완료 문구를 표시한다.

---

### 5.5 달력

예식이 있는 달의 달력을 표시한다.

* 일요일부터 토요일까지 요일 표시
* 해당 월의 날짜 표시
* 예식 날짜 강조
* 오늘 날짜를 강조할 필요는 없음
* 날짜 계산은 하드코딩하지 않고 실제 날짜를 기준으로 생성
* 모바일 화면에서 숫자와 간격이 깨지지 않도록 구성

예식 날짜 강조는 원형 배경이나 얇은 테두리 등 미니멀한 방식으로 처리한다.

---

### 5.6 사진 갤러리

웨딩 사진을 감상할 수 있는 갤러리를 구현한다.

필수 동작:

* 반응형 그리드 또는 정돈된 세로형 레이아웃
* 썸네일 클릭 시 확대 보기
* 전체 화면에 가까운 이미지 뷰어
* 이전 및 다음 사진 이동
* 닫기 버튼
* 현재 사진 번호 표시
* 배경 클릭 또는 `Escape` 키로 닫기
* 키보드 방향키 이동 지원
* 모바일 스와이프 지원이 가능하면 구현
* 이미지 비율이 달라도 레이아웃이 심하게 흔들리지 않도록 처리
* 이미지 지연 로딩 및 최적화
* 적절한 `alt` 텍스트 적용

이미지가 없을 때도 빌드가 실패하지 않도록 예시 이미지 또는 플레이스홀더 구조를 제공한다.

이미지는 `next/image`를 우선 사용한다.

---

### 5.7 오시는 길

다음 정보를 표시한다.

* 예식장 이름
* 홀 이름
* 도로명 주소
* 지번 주소가 있다면 함께 표시
* 주소 복사 버튼
* 지도 앱 연결 버튼
* 교통 안내
* 주차 안내

지원할 지도 버튼:

* 카카오맵
* 네이버지도
* T맵 또는 구글 지도

앱별 URL 스킴이나 웹 URL을 적절히 사용한다. 특정 지도 앱이 설치되지 않은 경우에도 가능한 한 웹페이지로 연결되도록 구성한다.

지도 서비스의 JavaScript SDK 키가 없는 상태에서도 페이지가 깨지지 않아야 한다. API 키가 필요한 지도를 사용한다면 환경변수를 이용하고, 키가 없을 때는 정적 지도 영역 또는 지도 앱 연결 버튼을 제공한다.

주소 복사가 완료되면 사용자에게 짧은 피드백을 표시한다.

예시:

```text
주소가 복사되었습니다.
```

---

### 5.8 교통 및 주차 안내

다음 내용을 구분해서 표시할 수 있도록 한다.

* 지하철
* 버스
* 자가용
* 주차
* 셔틀버스

각 항목은 설정 데이터가 존재할 때만 노출한다.

교통 정보가 많을 경우 접기·펼치기 방식 또는 아코디언을 사용할 수 있지만, 기본 정보는 쉽게 찾을 수 있어야 한다.

---

### 5.9 연락하기

신랑, 신부 및 양가 혼주에게 연락할 수 있는 기능을 구현한다.

지원 기능:

* 전화 걸기
* 문자 보내기
* 전화번호 복사
* 신랑 측과 신부 측 연락처 구분
* 연락처가 없는 사람은 버튼 미노출

모바일에서 전화 버튼을 누르면 `tel:` 링크가 정상 작동해야 한다.

연락처 목록은 모달 또는 하단 시트 형태로 표시할 수 있다. 접근성을 고려해 포커스 이동과 닫기 동작을 구현한다.

---

### 5.10 마음 전하실 곳

축의금을 전달할 계좌 정보를 표시한다.

신랑 측과 신부 측을 구분하고, 다음 정보를 제공한다.

* 예금주
* 은행명
* 계좌번호
* 계좌번호 복사 버튼

계좌번호는 처음부터 모두 노출하지 않고 접기·펼치기 방식으로 구성할 수 있다.

복사 후 다음과 같은 피드백을 표시한다.

```text
계좌번호가 복사되었습니다.
```

계좌 정보가 설정되지 않은 경우 해당 섹션 전체 또는 해당 인물의 항목을 숨긴다.

실제 송금 기능이나 결제 기능은 별도 요구가 없는 한 구현하지 않는다.

---

### 5.11 참석 여부 전달

하객이 참석 여부를 전달할 수 있는 RSVP 기능을 구현한다.

입력 항목:

* 참석 여부
* 신랑 측 또는 신부 측
* 이름
* 동행 인원
* 식사 여부
* 연락처
* 전달 메시지

필수 조건:

* 필수 입력값 검증
* 입력 오류 메시지 표시
* 중복 제출 방지를 위한 제출 중 상태
* 제출 성공 및 실패 안내
* 개인정보 입력에 대한 간단한 동의 문구
* 모바일 키보드에 적절한 입력 타입 지정
* 서버 저장 기능이 없다면 폼 UI만 허위로 동작시키지 말 것

백엔드 또는 데이터베이스가 없는 저장소라면 다음 중 프로젝트에 가장 적절한 방식을 선택한다.

1. Next.js Route Handler와 간단한 저장소 연동
2. Supabase 연동
3. Google Sheets 또는 외부 폼 연동
4. 설정값에 따라 RSVP 기능을 비활성화

외부 서비스를 사용한다면 환경변수와 설정 방법을 `README.md`에 작성한다. 환경변수가 없는 상태에서도 빌드가 실패하지 않아야 한다.

RSVP 기능 사용 여부는 설정 파일에서 켜고 끌 수 있도록 한다.

---

### 5.12 방명록

축하 메시지를 남기고 확인할 수 있는 방명록 기능을 구현한다.

입력 항목:

* 이름
* 비밀번호 또는 삭제용 PIN
* 축하 메시지

필수 기능:

* 메시지 목록 표시
* 최신순 정렬
* 메시지 작성
* 작성자 본인의 메시지 삭제
* 입력값 검증
* 빈 메시지 등록 방지
* 지나치게 긴 입력 제한
* 로딩, 빈 목록, 오류 상태 처리
* 스팸성 반복 제출 최소화

실제 저장소가 없는 경우, 저장되는 것처럼 보이는 가짜 방명록을 구현하지 않는다. 데이터베이스 연동이 어렵다면 설정에서 방명록을 끌 수 있도록 하고, 연동 방법을 문서화한다.

방명록 기능 사용 여부는 설정 파일에서 제어한다.

---

### 5.13 캘린더 일정 추가

하객이 결혼식 일정을 개인 캘린더에 추가할 수 있도록 한다.

가능하면 다음을 지원한다.

* 구글 캘린더 일정 추가
* `.ics` 파일 다운로드
* 일정 제목
* 시작 및 종료 시간
* 장소
* 청첩장 URL 또는 메모

시간대는 한국 표준시를 기준으로 처리한다.

---

### 5.14 공유하기

청첩장을 다른 사람에게 공유할 수 있도록 한다.

필수 기능:

* 브라우저 기본 공유 기능
* 링크 복사
* 카카오톡 공유는 설정이 준비된 경우 지원
* 공유 API를 지원하지 않는 환경에서는 링크 복사로 대체
* 복사 성공 피드백

공유되는 제목, 설명, 대표 이미지를 설정 파일에서 관리한다.

페이지 메타데이터와 Open Graph 정보를 구성하여 메신저에서 링크를 공유했을 때 대표 이미지와 설명이 자연스럽게 보이도록 한다.

---

### 5.15 배경 음악

배경 음악 기능을 선택적으로 제공한다.

필수 조건:

* 기본값은 자동 재생하지 않음
* 사용자가 직접 재생 버튼을 눌렀을 때만 재생
* 재생 및 일시정지 버튼 제공
* 현재 상태를 시각적으로 표시
* 접근 가능한 버튼 레이블 적용
* 음악 파일이 없으면 기능 미노출
* 설정에서 기능 활성화 여부 제어

브라우저의 자동 재생 제한을 우회하려는 코드는 작성하지 않는다.

---

### 5.16 푸터

페이지 마지막에는 다음 내용을 간결하게 표시한다.

* 신랑·신부 이름
* 감사 문구
* 결혼식 날짜
* 필요하면 제작 정보

예시:

```text
귀한 걸음으로 축복해 주셔서 감사합니다.
```

---

## 6. 고정 액션 영역

모바일 화면 하단에 주요 기능을 빠르게 사용할 수 있는 고정 액션 바를 구현할 수 있다.

추천 버튼:

* 연락하기
* 오시는 길
* 공유하기

다만 페이지 콘텐츠를 과도하게 가리거나 모바일 브라우저의 안전 영역과 겹치지 않도록 한다.

`env(safe-area-inset-bottom)`을 고려한다.

---

## 7. 반응형 요구사항

다음 환경에서 확인한다.

* 작은 모바일 화면
* 일반적인 모바일 화면
* 큰 모바일 화면
* 태블릿
* 데스크톱

기본 콘텐츠 영역은 모바일에서 화면 너비를 충분히 사용하고, 데스크톱에서는 지나치게 넓어지지 않도록 최대 너비를 둔다.

권장 콘텐츠 최대 너비는 약 480~720px 범위에서 디자인에 맞게 선택한다.

가로 스크롤이 발생하지 않아야 한다.

---

## 8. 접근성

다음 접근성 요구사항을 반영한다.

* 이미지에 의미 있는 대체 텍스트 제공
* 아이콘 버튼에 `aria-label` 제공
* 키보드만으로 주요 기능 사용 가능
* 포커스 표시 제거 금지
* 모달이 열리면 포커스를 내부에 유지
* 모달 닫기 후 기존 버튼으로 포커스 복귀
* 텍스트와 배경의 명암 대비 확보
* 색상만으로 상태를 구분하지 않기
* 입력 필드와 레이블 연결
* 오류 메시지를 명확하게 표시
* 스크린 리더가 이해할 수 있는 문서 구조 사용
* 제목 태그의 순서를 논리적으로 구성

---

## 9. 성능 최적화

* Next.js 이미지 최적화 사용
* 화면 밖 이미지 지연 로딩
* 대표 이미지에는 필요 시 우선순위 로딩 적용
* 불필요한 클라이언트 컴포넌트 최소화
* 큰 라이브러리를 단순 효과를 위해 추가하지 않기
* 애니메이션 라이브러리는 꼭 필요한 경우에만 사용
* 이미지 크기와 포맷 최적화
* 레이아웃 이동 최소화
* 폰트 로딩 최적화
* 번들에 불필요한 코드가 포함되지 않도록 구성

---

## 10. SEO 및 메타데이터

다음 메타데이터를 설정한다.

* 페이지 제목
* 페이지 설명
* Open Graph 제목
* Open Graph 설명
* Open Graph 대표 이미지
* Twitter 카드 정보
* 모바일 뷰포트
* 적절한 파비콘

예시 제목:

```text
김신랑 ♥ 이신부, 결혼합니다
```

청첩장은 공개 URL을 사용하는 경우가 많으므로 검색엔진 노출 여부를 설정으로 선택할 수 있게 한다.

검색 노출을 원하지 않는 경우 `robots` 메타데이터로 색인을 막을 수 있도록 한다.

---

## 11. 개인정보와 보안

* 실제 전화번호, 계좌번호, 개인 주소 등을 소스 코드 예시에 포함하지 않는다.
* 환경변수에 비밀 키를 직접 커밋하지 않는다.
* 클라이언트에 노출되면 안 되는 키는 서버에서만 사용한다.
* RSVP 및 방명록 입력값은 서버에서도 검증한다.
* HTML 입력값을 그대로 렌더링하지 않는다.
* 기본적인 요청 횟수 제한 또는 스팸 방지 방법을 고려한다.
* 개인정보 수집 항목은 꼭 필요한 수준으로 제한한다.
* 개인정보 보관 및 삭제 정책이 필요한 경우 문서에 안내한다.

---

## 12. 컴포넌트 구성 예시

프로젝트 상황에 맞게 조정하되, 대략 다음과 같이 역할을 분리한다.

```text
src/
├─ app/
│  ├─ layout.tsx
│  ├─ page.tsx
│  ├─ globals.css
│  └─ api/
│     ├─ rsvp/
│     └─ guestbook/
├─ components/
│  ├─ wedding/
│  │  ├─ CoverSection.tsx
│  │  ├─ InvitationSection.tsx
│  │  ├─ CoupleSection.tsx
│  │  ├─ WeddingDateSection.tsx
│  │  ├─ WeddingCalendar.tsx
│  │  ├─ GallerySection.tsx
│  │  ├─ ImageViewer.tsx
│  │  ├─ LocationSection.tsx
│  │  ├─ TransportationSection.tsx
│  │  ├─ ContactSection.tsx
│  │  ├─ AccountSection.tsx
│  │  ├─ RsvpSection.tsx
│  │  ├─ GuestbookSection.tsx
│  │  ├─ ShareSection.tsx
│  │  ├─ MusicPlayer.tsx
│  │  └─ WeddingFooter.tsx
│  └─ ui/
├─ config/
│  └─ wedding.ts
├─ lib/
│  ├─ date.ts
│  ├─ clipboard.ts
│  ├─ calendar.ts
│  └─ validation.ts
├─ types/
│  └─ wedding.ts
└─ public/
   ├─ images/
   └─ music/
```

현재 저장소의 구조가 다르다면 해당 구조를 존중하여 배치한다.

---

## 13. 상태 처리

비동기 기능에는 반드시 다음 상태를 구현한다.

* 기본 상태
* 로딩 상태
* 성공 상태
* 오류 상태
* 데이터가 없는 상태

버튼을 여러 번 눌러 중복 요청이 발생하지 않도록 한다.

토스트 알림을 사용해도 되지만, 간단한 기능을 위해 지나치게 큰 UI 라이브러리를 추가하지 않는다.

---

## 14. 테스트 및 검증

최소한 다음 항목을 직접 확인한다.

* 페이지가 정상적으로 빌드되는가
* 모바일에서 가로 스크롤이 발생하지 않는가
* 모든 이미지가 정상적으로 표시되는가
* 갤러리 확대 보기가 동작하는가
* 주소와 계좌번호 복사가 동작하는가
* 전화 및 문자 링크가 올바른가
* 지도 앱 링크가 올바른가
* 공유 및 링크 복사가 동작하는가
* D-Day 계산이 정확한가
* 달력의 날짜와 요일이 정확한가
* 예식일이 지난 경우에도 문구가 정상적인가
* 모달을 키보드로 열고 닫을 수 있는가
* 필수 폼 검증이 동작하는가
* RSVP와 방명록의 성공·실패 상태가 처리되는가
* 환경변수가 없는 상태에서도 가능한 범위에서 빌드가 되는가
* 데스크톱에서도 레이아웃이 지나치게 늘어나지 않는가
* 콘솔 오류와 경고가 없는가

가능한 경우 날짜 계산, 설정 데이터 검증, 주요 폼 검증 로직에 테스트를 추가한다.

---

## 15. 문서화

`README.md`에 다음 내용을 작성하거나 기존 내용을 갱신한다.

* 프로젝트 소개
* 실행 방법
* 설치 명령어
* 개발 서버 실행 방법
* 빌드 방법
* 청첩장 정보 수정 위치
* 이미지 교체 방법
* 음악 파일 추가 방법
* 지도 설정 방법
* RSVP 설정 방법
* 방명록 설정 방법
* 환경변수 목록
* 배포 방법
* 기능별 활성화 및 비활성화 방법

`.env.example`에는 필요한 환경변수 이름만 예시로 작성하고 실제 키는 포함하지 않는다.

---

## 16. 완료 조건

다음 조건을 모두 충족해야 작업이 완료된 것으로 본다.

* Next.js와 TypeScript로 구현되어 있다.
* 모바일 우선 반응형 디자인이 적용되어 있다.
* 깔끔하고 미니멀한 청첩장 분위기가 구현되어 있다.
* 메인 커버가 구현되어 있다.
* 초대 문구가 구현되어 있다.
* 신랑·신부 및 혼주 정보가 구현되어 있다.
* 예식 일시와 D-Day가 구현되어 있다.
* 예식 월 달력이 구현되어 있다.
* 사진 갤러리와 확대 보기가 구현되어 있다.
* 예식장 주소와 교통 안내가 구현되어 있다.
* 지도 앱 연결과 주소 복사가 구현되어 있다.
* 전화 및 문자 연락 기능이 구현되어 있다.
* 계좌 정보와 복사 기능이 구현되어 있다.
* 일정 추가 기능이 구현되어 있다.
* 공유 및 링크 복사 기능이 구현되어 있다.
* RSVP와 방명록은 실제 저장 방식으로 동작하거나 설정에서 명확히 비활성화할 수 있다.
* 배경 음악 기능은 선택적으로 사용할 수 있다.
* 모든 주요 정보는 중앙 설정 파일에서 변경할 수 있다.
* 접근성의 기본 요구사항이 반영되어 있다.
* 이미지와 폰트가 최적화되어 있다.
* 메타데이터와 Open Graph 정보가 설정되어 있다.
* 환경변수 예시와 사용 방법이 문서화되어 있다.
* lint, typecheck, build가 성공한다.
* 콘솔에 치명적인 오류가 없다.

---

## 17. Codex 작업 지시

먼저 저장소 전체 구조와 기존 코드를 확인한다.

그다음 이 문서의 요구사항을 기준으로 모바일 청첩장을 실제로 구현한다. 기존 코드와 충돌하는 부분이 있다면 현재 프로젝트 구조에 맞게 합리적으로 조정하되, 핵심 기능과 완료 조건은 빠뜨리지 않는다.

구현 순서는 다음과 같이 진행한다.

1. 기존 저장소와 기술 구성을 분석한다.
2. 필요한 타입과 청첩장 설정 데이터를 정의한다.
3. 공통 스타일과 전체 레이아웃을 구현한다.
4. 각 청첩장 섹션을 컴포넌트로 구현한다.
5. 갤러리, 복사, 연락, 지도, 공유, 캘린더 기능을 연결한다.
6. RSVP와 방명록의 저장 방식을 구현하거나 설정 가능한 형태로 구성한다.
7. 접근성, 반응형, 성능을 점검한다.
8. `README.md`와 `.env.example`을 작성한다.
9. lint, typecheck, test, build를 실행한다.
10. 발견된 오류를 수정한다.

작업을 중간 단계에서 멈추지 말고, 가능한 범위에서 완성된 상태까지 구현한다.

구현이 끝나면 다음 내용을 요약해서 보고한다.

* 새로 추가하거나 수정한 주요 파일
* 구현한 기능
* 사용자가 실제 정보로 교체해야 하는 위치
* 필요한 환경변수
* 실행한 검증 명령어와 결과
* 구현하지 못한 부분이 있다면 그 이유와 후속 작업
